package team16_project;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main class used as starting point for the application
 */
@SpringBootApplication public class Team16ProjectApplication {
	/**
	 * Main method that starts the spring application
	 * @param args Arguments from the command line
	 */
	public static void main(String[] args) {
		SpringApplication.run(Team16ProjectApplication.class, args);
	}
}
